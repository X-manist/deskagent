# 行为准则（Rules）

本目录是可注入到 `AGENTS.md` 的全局行为准则，约束助手的默认表现与安全边界。

| 文件 | 内容 |
|------|------|
| `base.md` | 基础行为：语言、工作方式、准确性、工具与执行 |
| `safety.md` | 安全合规：隐私凭据、内容合规、操作安全、专业边界 |

## 用法
桌面端在启动独立 agent home 时，会将以上内容写入 `AGENTS.md`，
使所有会话默认遵循这些准则。也可按需裁剪后合并到自定义系统提示中。
